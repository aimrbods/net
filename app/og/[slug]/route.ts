import {getPost} from '@/lib/api';import { SITE, escapeXML, sanitizeSlug } from '@/lib/config';

export const dynamic = 'force-static';
export async function generateStaticParams(){const posts=await (await import('@/lib/api')).getPosts();return [{slug:'home'},...posts.map(p=>({slug:sanitizeSlug(p.slug)})).filter(x=>x.slug!=='home')];}

export async function GET(_:Request,{params}:{params:Promise<{slug:string}>}){const {slug}=await params;const p=slug==='home'?null:await getPost(slug);const title=p?.title||SITE.name,k=(p?.kategori||'AI GACOR').toUpperCase(),themes=[['#7c3aed','#111827'],['#06b6d4','#0f172a'],['#ec4899','#111827'],['#22c55e','#111827'],['#f59e0b','#111827']],theme=themes[slug.length%themes.length];const fontSize=title.length>80?46:title.length>55?54:64;const svg=`<svg xmlns="http://www.w3.org/2000/svg" width="1200" height="630"><defs><linearGradient id="bg" x1="0" y1="0" x2="1" y2="1"><stop offset="0%" stop-color="${theme[0]}"/><stop offset="100%" stop-color="${theme[1]}"/></linearGradient></defs><rect width="1200" height="630" fill="url(#bg)"/><rect x="60" y="60" width="220" height="52" rx="26" fill="rgba(255,255,255,.12)"/><text x="90" y="94" fill="#fff" font-size="24" font-family="Arial" font-weight="700">🔥 ${escapeXML(k)}</text><text x="1140" y="92" text-anchor="end" fill="#fff" font-size="30" font-family="Arial" font-weight="800">⚡ ${escapeXML(SITE.name)}</text><foreignObject x="60" y="180" width="1050" height="300"><div xmlns="http://www.w3.org/1999/xhtml" style="font-size:${fontSize}px;font-family:Arial,sans-serif;font-weight:800;line-height:1.15;color:white;word-break:break-word">${escapeXML(title)}</div></foreignObject><text x="60" y="560" fill="#cbd5e1" font-size="26" font-family="Arial">${escapeXML(SITE.domain)}</text></svg>`;return new Response(svg,{headers:{'content-type':'image/svg+xml','cache-control':'public,max-age=86400'}})}
